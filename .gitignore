# ---------------------------------------------------------------
# The deployed artifact is zigi994/ and it is fully committed.
# Everything below is either regenerable, huge, or local-only.
# ---------------------------------------------------------------

# Original design source (~45 MB of .ai/PSD/render exports). The optimised
# WebP derivatives are committed under zigi994/assets/, so the originals
# stay off a public repo.
tools/src/
*.ai

# QA captures and intermediate pipeline output, all regenerable
tools/_qa/
tools/*.txt
tools/*-lqip.json

# Vendored pdf.js used only by the local rasterizer; re-downloadable
tools/pdf.*.mjs

# Source folders sitting next to the project on the desktop
交互设计/
ip设计/
室内设计/
广告设计/
ai设计/

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
*.log
